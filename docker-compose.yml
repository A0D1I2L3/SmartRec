services:
  smartrec:
    build: .
    ports:
      - '${PORT:-3001}:3001'
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - smartrec-data:/app/.data
    restart: unless-stopped

volumes:
  smartrec-data:
